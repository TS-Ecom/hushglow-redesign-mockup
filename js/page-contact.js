/* HushGlow Design — Contact page.

   No page-specific behaviour: the header, menus, cart drawer and footer all come from
   shared.js. The file exists so the include list is the same on every page. */
