/* HushGlow Design — Makeup Quizzes hub page.

   No page-specific behaviour: the press marquee, the product cards, the cart drawer and
   the menus all come from shared.js. The file exists so the include list is the same on
   every page. */
