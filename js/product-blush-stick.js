/* HushGlow Design — Multi-Effect Blush Stick page behaviors.

   Nothing is page-specific on this template: the gallery, swatches, accordions, bundle
   tiers with their offer timer and per-unit shade pickers, sliders, sticky ATC, lazy
   videos and the payment rows all come from shared.js. The file is kept so the include
   list is identical on every product page. */
