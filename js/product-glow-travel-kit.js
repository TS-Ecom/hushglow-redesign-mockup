/* HushGlow Design — Glow Travel Kit.

   Every behaviour on this page — gallery, swatches, accordions, upsell tabs, the puff
   toggle, sliders, sticky ATC and the lazy videos — comes from shared.js, because the
   page is assembled from blocks that already exist. Nothing is product-specific here
   yet; the file is kept so the page follows the same three-file shape as the others. */
