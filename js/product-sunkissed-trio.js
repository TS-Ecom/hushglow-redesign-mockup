/* HushGlow Design — Sunkissed Trio.

   All behaviour comes from shared.js. The two swatch rows need no page-specific code:
   the shared handler is wired per .swrow, so each row keeps its own selection and
   writes into the description line that follows it. */
